/**
 * 颜色常量，与 app.scss 中定义的主色、灰色等保持一致。
 * 用于 JS/TS 中需要传入具体色值的场景（如 NutUI theme、组件 props）。
 */

/** 主色，对应 --blue-6 / --primary-color-6 */
export const PRIMARY_COLOR = "#165dff";

/** 灰色 6，用于非激活态等，对应 --gray-6 */
export const GRAY_6 = "#86909c";
